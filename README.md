# Trading Bot - Модульная архитектура для анализа криптовалют и акций

Автоматизированный торговый бот для анализа криптовалютных и фондовых рынков с использованием технического анализа и ИИ.

## 🎯 Основные возможности

- **Мультирыночный анализ**: Поддержка криптовалют (Bybit) и акций (Tinkoff Investments)
- **Трехэтапный анализ**: Фильтрация → AI выбор → Комплексный анализ
- **Технические индикаторы**: EMA, RSI, MACD, ATR, Volume Profile, SMC паттерны
- **AI-анализ**: Интеграция с DeepSeek и Claude для принятия решений
- **Бектестинг**: Проверка сигналов на исторических данных
- **Telegram бот**: Удобный интерфейс для управления и получения сигналов

## 📁 Структура проекта

```
trading_bot/
├── main.py                 # Точка входа
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
│
├── prompts/               # Промпты для AI
│   ├── prompt_analyze.txt
│   ├── prompt_select.txt
│   ├── prompt_news_crypto.txt
│   └── prompt_news_stocks.txt
│
├── data_providers/        # Провайдеры данных
│   ├── bybit_client.py    # Bybit API (крипто)
│   ├── tinkoff_client.py  # Tinkoff API (акции)
│   ├── data_normalizer.py # Нормализация данных
│   └── market_data.py     # Рыночные данные
│
├── indicators/            # Технические индикаторы
│   ├── ema.py
│   ├── rsi.py
│   ├── macd.py
│   ├── volume.py
│   ├── atr.py
│   ├── correlation.py
│   ├── volume_profile.py
│   ├── order_blocks.py
│   ├── imbalance.py
│   ├── liquidity_sweep.py
│   ├── support_resistance.py
│   ├── false_breakout.py
│   ├── candle_patterns.py
│   └── news_analysis.py
│
├── stages/                # Этапы анализа
│   ├── stage1_filter.py   # Фильтрация по стратегии
│   ├── stage2_selection.py # AI выбор пар
│   └── stage3_analysis.py  # Комплексный анализ
│
├── ai/                    # AI провайдеры
│   ├── ai_router.py       # Роутер между провайдерами
│   ├── deepseek_client.py
│   └── anthropic_client.py
│
├── telegram/              # Telegram бот
│   ├── bot_main.py
│   ├── formatters.py
│   └── scheduler.py
│
├── utils/                 # Утилиты
│   ├── asset_detector.py  # Определение типа актива
│   ├── logger.py          # Логирование
│   ├── backtesting.py     # Бектестинг
│   ├── signal_storage.py # Хранение сигналов
│   └── validators.py     # Валидация
│
├── logs/                  # Логи (автосоздается)
└── signals/               # Сигналы (автосоздается)
    └── backtest_results/
```

## 🚀 Установка

### Требования

- Python 3.11+
- API ключи:
  - DeepSeek API или Anthropic API
  - Telegram Bot Token
  - Tinkoff Invest Token (для акций)
  - Bybit API (опционально, для расширенных данных)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

Для работы с акциями дополнительно:
```bash
pip install t-tech-investments
```

### Настройка

1. Создайте файл `.env` в корне проекта:

```env
# AI Providers
DEEPSEEK_API_KEY=your_deepseek_key
ANTHROPIC_API_KEY=your_anthropic_key

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_USER_IDS=123456789,987654321
TELEGRAM_ADMIN_IDS=123456789
TELEGRAM_GROUP_ID=-1001234567890

# Tinkoff Investments (для акций)
TINKOFF_INVEST_TOKEN=your_tinkoff_token

# AI Configuration
STAGE2_PROVIDER=deepseek
STAGE3_PROVIDER=deepseek
```

2. Настройте параметры в `config.py` при необходимости

## 📖 Использование

### Запуск Telegram бота

```bash
python main.py
```

или

```bash
python main.py telegram
```

### Одноразовый анализ

```bash
python main.py once
```

### Команды Telegram бота

- `/start` - Запустить бота
- `▶️ Запустить сейчас` - Запустить анализ
- `🔍 Анализ пары` - Анализ конкретной пары
- `📊 Статистика` - Статистика сигналов
- `📊 Backtest` - Запустить бектестинг

## 🔧 Архитектура

### Принципы модульности

1. **Разделение по типам активов**: Автоматическое определение через `AssetTypeDetector`
2. **Единый формат данных**: `NormalizedCandles` для всех индикаторов
3. **Централизованное определение типа**: `AssetTypeDetector`
4. **Модульные индикаторы**: Каждый индикатор в отдельном файле
5. **Универсальные провайдеры**: Автоматическое определение типа актива

### Поток данных

```
1. Загрузка пар (автоматическое определение типа)
   ↓
2. Stage 1: Фильтрация по стратегии False Breakout
   ↓
3. Stage 2: AI выбор лучших пар
   ↓
4. Stage 3: Комплексный анализ с новостями и корреляцией
   ↓
5. Сохранение сигналов и отправка в Telegram
```

### Определение типа актива

Бот автоматически определяет тип актива по символу:
- **Крипто**: заканчивается на USDT, BUSD, USDC и т.д.
- **Акции**: все остальное

Используется `utils/asset_detector.py` для централизованного определения.

## 📊 Этапы анализа

### Stage 1: Фильтрация

- Поиск уровней поддержки/сопротивления
- Детекция ложных пробоев
- Анализ свечных паттернов
- Фильтрация по confidence

**Выход**: Список кандидатов с confidence >= 60

### Stage 2: AI Выбор

- Загрузка 1H и 4H свечей
- Расчет полных индикаторов с историей
- Анализ SMC паттернов
- AI выбор 3-5 лучших пар

**Выход**: Список выбранных символов

### Stage 3: Комплексный анализ

- Загрузка расширенной истории
- Анализ новостей (72 часа)
- Корреляция с BTC (крипто) или MOEX (акции)
- Volume Profile анализ
- SMC паттерны с историей
- AI анализ и генерация сигнала

**Выход**: Торговые сигналы с entry, stop, TP уровнями

## 🧪 Бектестинг

Бектестинг автоматически определяет тип актива и загружает свечи из соответствующего источника:

- **Крипто**: Bybit API (5-минутные свечи)
- **Акции**: Tinkoff API (5-минутные свечи)

```bash
# Через Telegram
📊 Backtest

# Или программно
from utils.backtesting import get_backtester
backtester = get_backtester()
results = await backtester.run_backtest(signals)
```

## 📝 Логирование

Логи сохраняются в `logs/`:
- `bot_YYYYMMDD.log` - все логи
- `bot_errors_YYYYMMDD.log` - только ошибки

Уровни логирования:
- **INFO**: Основные этапы, результаты
- **DEBUG**: Детальная информация (отключено по умолчанию)
- **ERROR**: Ошибки

## 🔍 Добавление новых компонентов

### Новый индикатор

1. Создайте файл `indicators/my_indicator.py`
2. Используйте `NormalizedCandles` как вход
3. Возвращайте `@dataclass` с результатами
4. Экспортируйте через `indicators/__init__.py`

### Новый провайдер данных

1. Создайте клиент в `data_providers/`
2. Используйте `normalize_candles()` для нормализации
3. Добавьте в универсальную функцию `fetch_candles()`

### Новый AI провайдер

1. Создайте клиент в `ai/`
2. Реализуйте методы `select_pairs()` и `analyze_comprehensive()`
3. Зарегистрируйте в `ai/ai_router.py`

## ⚙️ Конфигурация

Основные параметры в `config.py`:

- **Таймфреймы**: `TIMEFRAME_SHORT` (1H), `TIMEFRAME_LONG` (4H)
- **Индикаторы**: Периоды EMA, RSI, MACD
- **Пороги**: `MIN_CONFIDENCE`, `MIN_VOLUME_RATIO`
- **AI**: Провайдеры, модели, температура
- **Batch загрузка**: Лимиты для оптимизации

## 🐛 Решение проблем

### Ошибка "Tinkoff not available"

Установите библиотеку:
```bash
pip install t-tech-investments
```

### Ошибка загрузки свечей

Проверьте:
- API ключи в `.env`
- Доступность API (Bybit/Tinkoff)
- Интернет соединение

### Медленная загрузка акций

Batch загрузка оптимизирована для 300 тикеров. Прогресс логируется каждые 50 элементов.

## 📚 Дополнительная документация

- `ARCHITECTURE.md` - Детальная архитектура и правила масштабирования
- `config.py` - Все параметры с комментариями
- Документация в docstrings каждого модуля

## 📄 Лицензия

Проект для личного использования.

## 🤝 Вклад

При добавлении новых функций следуйте принципам модульности:
1. Разделение по типам активов
2. Использование `NormalizedCandles`
3. Централизованное определение типа актива
4. Модульные индикаторы
5. Информативное, но не избыточное логирование
